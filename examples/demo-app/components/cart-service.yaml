id: COMP-cart-service
name: Cart Service

owners:
  - team-platform

paths:
  - services/cart/**

interfaces:
  provides:
    - API-CART-V1
  consumes:
    - API-CATALOG-V1

dependency_policy:
  allowed_components:
    - COMP-catalog-service

attributes:
  tier: 2
  data: [session]
  runtime: [docker]
